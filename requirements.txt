# Pinned to what produced the Phase 1 gate result (DISCUSSION D-14, 2026-09-07).
# Python 3.13.

# Core
numpy==2.4.1
pandas==2.3.3
pyarrow==23.0.1

# Models (gradient boosting only — no deep learning frameworks, CLAUDE.md)
scikit-learn==1.8.0
xgboost==3.2.0

# Gridded data access
xarray==2026.7.0
zarr==3.3.0
numcodecs==0.16.5
h5py==3.16.0
fsspec==2026.3.0
aiohttp==3.13.5
huggingface-hub==1.20.1

# IMD gridded rainfall
imdlib==0.1.21

# HTTP (index endpoints)
requests==2.32.5

# WhatsApp delivery (Twilio Sandbox -- free, no business verification, RESEARCH.md)
twilio==9.11.0
